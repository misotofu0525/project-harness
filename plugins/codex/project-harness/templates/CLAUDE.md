@AGENTS.md

## Claude Code

- Use `.claude/rules/` only for genuinely path-specific instructions.
- Every project rule must include `paths` frontmatter; do not add an unscoped rule that becomes permanent context.
