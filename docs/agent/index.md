# Project Context Index

## Architecture

Read `architecture.md` when:

- changing shared script boundaries or Journal semantics
- changing repository layout or lifecycle flows
- adding a new durable capability

## Distribution

Read `distribution.md` when:

- changing plugin manifests, skills, hooks, or packaged runtime assets
- changing the shared-to-plugin synchronization contract
- preparing plugin validation or release work

## Verification

Read `verification.md` when:

- changing scripts, templates, tests, or verification commands
- preparing explicit task completion

## Research

Read `../research/index.zh.md` when:

- a task depends on earlier harness-engineering research
- adding or synthesizing durable research output

Load only the entries whose conditions match the current task.
